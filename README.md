# 국내사업부 폭염 현황판 — v4

`repo-v2`(v3) 에서 화면 배치와 자동 확정을 손본 판입니다.

| 파일 | 설명 |
|---|---|
| `index.html` | 현황판 본체 (단일 파일) |
| `api/kma.js` | 기상청 API 중계함수 |
| `FIREBASE_RULES.txt` | Firebase 보안규칙 — **먼저 적용하세요** |

## 화면

```
국내사업부 주요거점 폭염 현황   ● 공용 저장소 연결됨   [조회 전용] [사용자 코드]•••• [확인]

              [ ↻ 새로고침 ]

┌─ 복사 대상 ─────────────────────────────────────────────┐
│ 폭염현황  (2026.07.30 14시 기준)                          │
│   폭염중대경보 1개소  부산신항                              │
│   폭염경보 3개소     대전, 신탄진, 신탄진 CY                 │
│   최고 체감온도 33.5℃  군산                                │
│                                                          │
│ 거점별 현재 기상 · 체감온도    폭염특보 ■없음 ■주의보 ■경보 ■중대경보│
│  ┌────┐┌────┐┌────┐┌────┐┌────┐                          │
│  │군산 ││장항 ││대전 ││천안 ││신탄진│   ← 가로 5개 × 2줄       │
│  └────┘└────┘└────┘└────┘└────┘                          │
│  ┌────┐┌────┐┌────┐┌────┐┌────┐                          │
│  │부산 ││신탄││군산││아산││의왕 │                            │
│  │신항 ││진CY││ CY ││ CY │      │                          │
│  └────┘└────┘└────┘└────┘└────┘                          │
│  기타사항 │ 전 거점 생수 지급 중                             │
└──────────────────────────────────────────────────────────┘

[✔ 폭염현황 확정]  [📋 복사]        최근 확정 2026-07-30 00:01 · 자동(00시)

▶ 확정 이력  4건                          ← 접이식
```

## v3 에서 달라진 점

| | v3 | v4 |
|---|---|---|
| 카드 음영 | 폭염특보 단계별 (노랑·주황·적색) | **전 거점 동일 — 옅은 하늘색 `#e8f4fc`** |
| 폭염특보 표시 | 11.5px 배지 | **14px 배지** (단계별 색은 유지) |
| 배치 | 계열 6 / 컨테이너 4 그룹 | **가로 5개씩 2줄** (그룹 구분 없음) |
| 입력 글자 | 12px 보통 | **13.5px bold** (기타사항 14.5px bold) |
| 복사 버튼 | 작은 버튼 | **새로고침과 동일 크기·형태** (260px) |
| 복사 범위 | 거점 영역만 | **폭염현황 + 거점별 현재 기상 한 화면** |
| 확정 이력 | 항상 펼침 | **접이식** (기본 접힘) + **삭제 가능** |
| 자동 저장 | 없음 | **매일 00:00 전일자 자동저장** (오늘 값은 유지) |

폭염특보 단계는 카드 배경이 아니라 **배지 색**으로 구분합니다. 범례가 그 색을 설명합니다.

## 확정 저장

### 수동 — `✔ 폭염현황 확정`

그 시점의 기상현황·체감온도·폭염특보·입력내용 전체를 스냅샷으로 저장합니다.
**입력란의 값은 그대로 유지됩니다.** 몇 번을 눌러도 이력에 쌓이기만 합니다.

입력창을 열어둔 채로 확정을 눌러도 그 값이 먼저 저장된 뒤 스냅샷에 담깁니다.

### 자동 — 매일 00:00

자정이 지나면 **전일자** 자료를 확정 스냅샷으로 남기고, **그 전일자의 입력란만** 비웁니다.
오늘 입력 중인 값은 절대 건드리지 않습니다.

- 확정자는 `자동(00시)` 로 기록됩니다.
- 자정에 아무도 화면을 켜두지 않았어도, 다음 접속 때 전일자를 찾아 처리합니다.
- 여러 PC가 켜져 있어도 하루 한 번만 저장되도록 `/autofix/{날짜}` 에 표식을 먼저 남깁니다.
- 30초 주기로 감시합니다.

### 이력 조회 · 삭제

`확정 이력` 에서 `보기` 를 누르면 **전 거점이 표로** 펼쳐집니다.
(기상·기온·체감·폭염특보·근로자·제품(시설)·운송 + 기타사항)

잘못 저장된 기록은 **사용자 코드 입력 상태에서 `삭제`** 로 지울 수 있습니다.
조회 전용에서는 삭제 버튼이 보이지 않고 우회 호출도 막힙니다. 삭제는 로그에 남습니다.

## 사용자 코드

| 코드 | 결과 |
|---|---|
| `admin` | 전 거점 입력·수정 + 확정 |
| (미입력) | 조회 전용 |

`index.html` 의 `const EDIT_CODE='admin';` 한 줄입니다.

## 입력 방법

칸을 클릭하면 팝업 없이 그 자리에서 입력창이 열립니다.
`Enter` 저장 · `Shift+Enter` 줄바꿈 · `Esc` 취소 · 다른 곳 클릭 시 저장.

## ⚠ Firebase 보안규칙

`autofix` 경로가 추가되었습니다.

```json
{
  "rules": {
    "days":      { ".read": true, ".write": true },
    "logs":      { ".read": true, ".write": true },
    "confirmed": { ".read": true, ".write": true },
    "autofix":   { ".read": true, ".write": true }
  }
}
```

`autofix` 가 없어도 자동확정은 동작하지만, 브라우저별로 각각 저장돼 중복될 수 있습니다.

---

©2026 김원상 All rights reserved.
